# Dependencies
# This folder contains all the project's third-party libraries.
# It can be regenerated by running 'npm install' or 'yarn install'.
/node_modules

# Build output
# This directory contains the compiled and bundled code for production.
# It is generated by running a build command like 'npm run build'.
/dist

# System files
# These are files created by the operating system (e.g., macOS).
.DS_Store

# Log files
# These files are often generated by package managers for debugging.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
# These files store sensitive information like API keys and should not be committed.
# A .env.example file is often committed instead.
.env
.env.local
.env.*.local

# IDE and editor directories
.vscode/
.idea/
